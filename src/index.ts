/**
 * @particle-academy/fancy-motion — the scroll-timeline motion engine.
 *
 * The page is a video; scroll is the playhead. This entry is the pure,
 * framework-agnostic core: the TimelineDoc model + the snapshot tween engine.
 * The React runtime player lives at `./react`.
 */
export * from "./timeline/types";
export * from "./engine/interpolate";
